# FastAPI MVC CRUD API – Item Management

A simple REST API built with **FastAPI**, following strict **MVC** architecture. 
It provides full CRUD operations for managing items, uses **SQLite** as the database, and includes automatic interactive documentation.

## 📁 Project Structure (MVC)